/*apc means alert prompt confirm*/
//alert('Hi');We already know what this does
//let result = prompt('The text you input is assigned to the result variable', 'Text here please'); but we should let this run
//confirm(This is a confirm statement?), but we already know what this does.
let apcLog = 'apc.js worked, despite it being all comented it out'; console.log(apcLog);